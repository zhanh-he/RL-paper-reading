{
  "name": "rl-paper-reading",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/sync-catalog.mjs",
    "check": "node scripts/sync-catalog.mjs --check",
    "serve": "python3 -m http.server 4173 --directory site"
  }
}
